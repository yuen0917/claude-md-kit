<!--
═══════════════════════════════════════════════════════════════
通用 CLAUDE.md 範本
使用方式:
1. 複製本檔為你專案根目錄的 CLAUDE.md。
2. 將所有 <角括號> placeholder 換成專案實際內容。
3. 刪除所有 <!── ──> 註解(它們是給「人」看的填寫說明,
   留著只會浪費 agent 的 context)。
4. 用 CHECKLIST.md 自我檢核;成品目標:60–80 行以內。

每一行去留的唯一判斷準則:
「如果刪掉這行,agent 會不會因此出錯?」不會 → 刪。
═══════════════════════════════════════════════════════════════
-->

# <專案名稱>

<!-- 用一句話給 agent 領域脈絡:專案是什麼、給誰用、處於什麼階段。
     缺了它,agent 會在模糊處做出方向錯誤的假設。 -->
<一句話描述,例如:面向 X 使用者的 Y 服務,目前處於 MVP 階段,尚未上線>

## 環境與技術棧
<!-- 只寫「從目錄結構看不出來」的部分:版本限制、指定的套件管理器、
     環境變數來源。agent 打開 repo 就能看見的東西不要寫。 -->
- 語言/框架:<例如 Python 3.12 + FastAPI>
- 套件管理:<例如 uv;新增依賴用 `uv add`,禁止直接 pip install>
- 環境變數:讀自 `.env`(不入版控);缺少時先停下告知,不要硬編碼替代值。

## 驗證指令
<!-- 全檔最重要的區塊:給 agent 一個「可執行的完成定義」。
     指令必須實際跑過、可直接複製執行。 -->
- Test:`<test 指令>`
- Lint/Typecheck:`<lint / typecheck 指令>`
- Build:`<build 指令;無則刪此行>`
- 本地執行:`<run 指令>`

IMPORTANT:任務只有在 Test 與 Lint/Typecheck 全數通過後才算完成;「程式碼寫完」不等於完成。

## 架構地圖
<!-- 只列高層目錄與職責(5–10 行),幫 agent 快速定位該去哪裡改。
     細節放 docs/ 並註明「何時去讀」,不要用 @ 把整份文件嵌進來。 -->
- `<dir1>/` — <職責>
- `<dir2>/` — <職責>
- `<dir3>/` — <職責>
- 需要動 <核心模組/資料模型> 時,先讀 `docs/ARCHITECTURE.md`。

## 慣例與陷阱
<!-- 只寫「違反會出錯、且工具擋不住」的事:架構慣例、易踩的坑、歷史包袱。
     formatter / linter 已能強制的格式規則不要寫(改用 hook)。 -->
- <慣例,例如:所有 DB 存取一律經過 repository 層,route 不直接下 query>
- <陷阱,例如:integration 測試依賴本地 docker,先跑 `docker compose up -d`>
- <歷史包袱,例如:legacy/ 目錄勿動,預計 Q4 淘汰>

## 跨 session 狀態
<!-- 長期專案的單一事實來源,讓新 session 能接續前一個 session 的脈絡。
     配套的 STATE.md 結構見 WORKFLOW.md。 -->
- 開工前:讀 `docs/STATE.md`(目前目標、進度、未決事項、關鍵決策)。
- 收工前:更新 `docs/STATE.md`(本次變更、新決策與理由、下一步)。

## 邊界
<!-- 少數不可違反的紅線。「絕不」等強標記全檔合計不要超過 3 處,
     濫用會稀釋效果。 -->
- 絕不將秘密(API key、憑證)寫入任何會被讀取或提交的檔案;一律走環境變數。
- <專案紅線,例如:絕不直接對 production DB 跑 migration;先產出 SQL 供人審核>
